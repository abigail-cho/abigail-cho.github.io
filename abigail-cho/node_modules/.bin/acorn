#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@docusaurus/utils/node_modules/file-loader/node_modules/acorn/bin/acorn" "$@"
  ret=$?
else 
  node  "$basedir/../@docusaurus/utils/node_modules/file-loader/node_modules/acorn/bin/acorn" "$@"
  ret=$?
fi
exit $ret
